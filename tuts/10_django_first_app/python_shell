
python3 manage.py shell

from blog.models import Post
Post.Status.choices
Post.Status.labels
Post.Status.values
Post.Status.names

# django shell similar to python shell except it reads settings.py already and preloads stuff
